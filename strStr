
class Solution {
public:
    int strStr(string haystack, string needle) {
         int size =needle.size();
         for(int i=0;i<haystack.size();i++)
         {
             if(needle==haystack.substr(i,size)){ // if needle part of haystack
                 return i;
             }
         }
        return -1;
    }
};
